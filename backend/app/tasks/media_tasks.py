"""
Media processing tasks (placeholder for core AI pipeline).

Image validation (YOLO11), voice transcription (Whisper/Groq),
text formalization (Gemini), and embedding generation (BGE-M3)
will be implemented here as part of the core AI pipeline.
"""

# These are core AI features — deferred to the AI pipeline build phase.
# The ComplaintProcessingStatus model tracks the state of each step:
#   transcription_status, formalization_status,
#   image_validation_status, embedding_status
